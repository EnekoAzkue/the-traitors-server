
import { KaotikaCharacters } from "../../../helpers/constants/constants";
import { Locations } from "../../interfaces";

export default interface Angelo {
  name        : KaotikaCharacters.ANGELO,
  isCaptured  : boolean,
  location    : Locations,
};