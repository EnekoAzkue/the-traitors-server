services:
  server:
    build: .
    container_name: team2-server
    ports:
      - "6001:3000"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./firebase-service.json:/usr/src/app/firebase-service.json
      - ./server.key:/usr/src/app/server.key
      - ./server.crt:/usr/src/app/server.crt
      - ./ca.crt:/usr/src/app/ca.crt

